apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cps-and-ncmp.release-name" . }}
  labels:
    app: {{ include "cps-and-ncmp.name" . }}
    component: cps-and-ncmp
spec:
  replicas: {{ .Values.cps.replicas }}
  selector:
    matchLabels:
      app: {{ include "cps-and-ncmp.name" . }}
      component: cps
  template:
    metadata:
      labels:
        app: {{ include "cps-and-ncmp.name" . }}
        component: cps
    spec:
      serviceAccountName: cps-hazelcast-service-account
      initContainers:
        # Wait for PostgreSQL and Kafka to be ready
        - name: wait-for-dependencies
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              echo "Waiting for PostgreSQL..."
              for i in $(seq 1 60); do
                if nc -z {{ include "cps-and-ncmp.release-name" . }}-postgresql-service 5432; then
                  echo "PostgreSQL is ready"
                  break
                fi
                echo "PostgreSQL not ready, waiting... ($i/60)"
                sleep 2
              done
              echo "Waiting for Kafka..."
              for i in $(seq 1 60); do
                if nc -z {{ include "cps-and-ncmp.release-name" . }}-kafka-service 9092; then
                  echo "Kafka is ready"
                  break
                fi
                echo "Kafka not ready, waiting... ($i/60)"
                sleep 2
              done
              echo "All dependencies are ready"
      containers:
        - name: cps
          image: "{{ .Values.cps.image.repository }}:{{ .Values.cps.image.tag }}"
          imagePullPolicy: {{ .Values.cps.image.pullPolicy }}
          ports:
            - containerPort: 8080
            - containerPort: 5701
          env:
            {{- range $envName, $envValue := .Values.cps.env }}
            - name: {{ $envName | upper }}
              value: "{{ $envValue }}"
            {{- end }}
            - name: CPS_NCMP_SERVICE_NAME
              value: cps-{{ include "cps-and-ncmp.name" . }}-hazelcast-service
          resources:
            {{- toYaml .Values.cps.resources | nindent 12 }}
          livenessProbe:
            {{- toYaml .Values.cps.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.cps.readinessProbe | nindent 12 }}
